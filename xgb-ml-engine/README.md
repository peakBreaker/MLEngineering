## Getting started with XGBoost on ML engine

- Install the requirements `$ pip install -r requirements.txt`
- Train the model : `$ ./train_model.py`
- Source the utility script and follow its instructions : `$ source infra.sh`
